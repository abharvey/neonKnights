import styled from "styled-components";

import { centeredFlexColumn } from "./cssUtils";

const ContentPanel = styled.div`
    ${centeredFlexColumn};
    width: 100vw;
    min-height: 10vh;
`;

export default ContentPanel;
